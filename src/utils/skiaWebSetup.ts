/**
 * skiaWebSetup.ts
 *
 * Native stub (iOS / Android / Expo Go).
 * CanvasKit is not used on native — Skia renders via the native GPU module.
 * This file intentionally does nothing; it exists only so that the
 * @/utils/skiaWebSetup import in _layout.tsx resolves on native without error.
 */
export {};
